# Q2 程式設計與物件導向 / OOP Class Architecture

---

## 設計前提假設

本架構假設 CAD 圖面已完成以下前處理：

- 各構件以獨立圖塊（BLOCK）或帶有自定義屬性的實體表示，而非分解的線條或點元素
- 圖塊插入點已對齊座標系原點（可透過 AutoLISP 工具批次校正，如 LeeMac 的 Change Block Base Point，或於讀取時以原點偏移量補正）
- 圖面原點座標已與 3D 模型座標系對齊
- 構件類型依圖層命名規則識別，各構件類型使用獨立圖層，避免混用預設圖層（如圖層 0）

此外，本架構以理想化條件為前提，略去以下平台差異細節：Slab 對應 Revit 中的 Floor 類別；Column 在 Revit 中進一步區分為結構柱（Structural Column）與建築柱（Architectural Column）。實際對接時，子類別命名與 JSON key 可依目標平台規格調整。

> 以下參數僅為示意，目的在表達架構的共用性、可擴充性、預設值與 key mapping 的設計概念。實際實作時，幾何參數的數量與型別會依圖面規範、座標系定義及下游模組接收格式而有所不同。構件識別、圖層解析、異常處理等前處理邏輯均未納入本範疇。

---

## Q2-1 類別架構圖 / Class Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                父類別：BuildingElement                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 共用屬性                                                  │   │
│  │   element_id   圖元識別碼，對應 CAD Entity Handle         │   │
│  │   layer        所在圖層名稱                               │   │
│  │   level        所在樓層                                   │   │
│  │   material     材質（可選）                               │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 方法                                                      │   │
│  │   _key_mapping()          預設標籤對照表（可覆寫）         │   │
│  │   _geometry_payload()     幾何資料（抽象，強制子類實作）   │   │
│  │   to_dict()               統一 JSON 輸出入口              │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                    │ 繼承（Inheritance）
        ┌───────────┼───────────┐
        ▼           ▼           ▼
┌────────────┐ ┌────────────┐ ┌────────────┐
│  Column    │ │   Wall     │ │   Slab     │
│  （柱）    │ │  （牆）    │ │  （樓板）  │
├────────────┤ ├────────────┤ ├────────────┤
│ origin     │ │start_point │ │boundary_pts│
│ height     │ │end_point   │ │thickness   │
│section_w   │ │height      │ │elevation   │
│section_d   │ │thickness   │ │opening_ids │
│ rotation   │ │base_elev   │ │slope       │
├────────────┤ ├────────────┤ ├────────────┤
│key mapping │ │key mapping │ │key mapping │
│覆寫：      │ │沿用預設值  │ │沿用預設值  │
│element_id  │ │            │ │            │
│→ col_id   │ │            │ │            │
└────────────┘ └────────────┘ └────────────┘
        │           │           │
        └───────────┴───────────┘
                    │
                    ▼
        ┌─────────────────────┐
        │  JSON 輸出          │
        │  → 交付下游 3D 模組  │
        └─────────────────────┘
```

---

## Q2-2 架構選擇理由

### ① 單一職責原則（Single Responsibility Principle）

共用屬性集中在父類別管理；各構件的幾何邏輯只在自己的子類別內定義，兩者職責不混用。修改其中一方不會連動另一方。

### ② 開放封閉原則（Open/Closed Principle）

日後新增構件類型（如 Beam、Door）只需新增子類別，不需修改父類別或既有子類別的任何程式碼。架構對擴充開放、對修改封閉。

### ③ 強制契約（Abstract Method）

父類別的 `_geometry_payload()` 宣告為抽象方法，強制每個子類別都必須實作自己的幾何輸出邏輯。未實作則在執行前就會報錯，不會等到輸出時才發現問題。

### ④ 解耦：資料內容與輸出格式分離

`_key_mapping()` 的存在將「資料內容（屬性值）」與「輸出欄位命名（JSON key）」分成兩層：

| 層次 | 說明 | 穩定性 |
|------|------|--------|
| 資料層（屬性值） | element_id、layer 等實際資料 | 穩定，不隨下游需求變動 |
| 命名層（JSON key） | 輸出時貼上的標籤名稱 | 易變，各下游模組可能要求不同命名 |

---

## Q2-3 Column JSON key 改名的影響範圍

當下游模組要求 Column 的 `element_id` 改輸出為 `col_id`，而 Wall 與 Slab 不受影響時：

- 只需在 `Column` 子類別內覆寫 `_key_mapping()` 方法
- 將其中 `"element_id"` 的輸出標籤改為 `"col_id"`
- `Wall` 與 `Slab` 未覆寫此方法，繼續沿用父類別預設值，零異動
- 父類別的 `to_dict()` 邏輯一行未動

改動範圍嚴格限制在 `Column` 子類別的單一方法內。這正是 `_key_mapping()` 設計為可覆寫的核心理由：讓格式需求的變動不會擴散到整體架構。

---

## 檔案說明

| 檔案 | 說明 |
|------|------|
| `README.md` | 架構說明、設計理由、Q2-1~Q2-3 回答 |
| `building_elements.py` | 類別定義與主程式實作 |
